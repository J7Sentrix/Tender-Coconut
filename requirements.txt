# Tender Coconut — Python Dependencies
# Install with: pip install -r requirements.txt

# ── CLI & Terminal UI ─────────────────────────────────────────────────────────
click>=8.1.7
rich>=13.7.0

# ── HTTP / Networking ─────────────────────────────────────────────────────────
requests>=2.31.0
urllib3>=2.0.0

# ── LLM Backends (install only what you need) ─────────────────────────────────
anthropic>=0.30.0          # Claude (Anthropic)
google-generativeai>=0.7.0 # Gemini (Google)
openai>=1.35.0             # Grok (xAI) — uses OpenAI-compatible client
ollama>=0.2.0              # Ollama (local models)

# ── Optional: Report export ───────────────────────────────────────────────────
# jinja2>=3.1.4            # HTML report templating (future)
# tabulate>=0.9.0          # Tabular output alternatives
